name: Build OneCloud ImmortalWrt

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "immortalwrt/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 启用 QEMU，支持在 x86 上运行 ARMv7 镜像
      - name: Enable QEMU for ARM
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm,arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build firmware
        run: |
          cd immortalwrt
          chmod +x docker-build.sh build.sh || true
          ./docker-build.sh

      - name: Show build result
        run: |
          echo "=== Build Output ==="
          ls -lhR immortalwrt/bin || true

      # 删除旧 release
      - name: Delete old release
        uses: dev-drprasad/delete-older-releases@v0.3.4
        with:
          keep_latest: 0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 上传最新线刷包到 release
      - name: Upload new release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "release-${{ github.run_number }}"
          name: "OneCloud ImmortalWrt Build #${{ github.run_number }}"
          body: |
            🆕 更新内容：
            - 更新内核、插件或功能改动请查看提交记录。
            - 自动生成线刷包文件：`immortalwrt-ext4-emmc-burn.gz`
            - 仅保留最新构建版本。
          files: |
            immortalwrt/bin/targets/*/*/immortalwrt-ext4-emmc-burn.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
